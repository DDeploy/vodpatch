# Normalise on commit, check out native.
* text=auto

# cmd.exe is unreliable with LF-only batch files: always check these out CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# PowerShell 5.1 is happier with CRLF too.
*.ps1 text eol=crlf

*.md  text
